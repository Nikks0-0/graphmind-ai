{
  "node": {
    "id": "string (unique hash)",
    "label": "string (concept name, e.g., 'Transformer Architecture')",
    "type": "string (entity category)",
    "summary": "string (AI-generated brief)"
  },
  "edge": {
    "source": "string (node_id)",
    "target": "string (node_id)",
    "relationship": "string (e.g., 'DEPENDS_ON', 'IMPLEMENTS')"
  }
}
